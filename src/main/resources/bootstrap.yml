spring:
    cloud:
        config:
            uri: http://user:${CONFIG_PASSWORD}@${CONFIG_SERVER_ADDRESS}:${CONFIG_SERVER_PORT}
            name: cronus-application
            profile: ${CONFIG_PROFILE}